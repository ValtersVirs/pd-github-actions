name: "Install dependencies"
description: "Composite action to install dependecies"
runs:
  using: composite
  steps:
    - name: Print step name
      run: echo "Installing dependencies has started.."
      shell: cmd